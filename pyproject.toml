[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "integritee_cli_py"
version = "0.1.0"
description = "Python wrapper for the Integritee RPC client"
readme = "README.md"
license = "MIT"

